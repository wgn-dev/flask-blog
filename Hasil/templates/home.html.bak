<!DOCTYPE html>
<html>
	<head>
		<title>Home</title>
	</head>
	<body>
		<ol>Application
		<form method="post">
			{%for i in range(0, len)%}
				<li><input type="button" name={{Dokumen[i]}} class={{Dokumen[i]}} value={{Dokumen[i]}}/></li>
			{%endfor%}
		</form>
		</ol>
	</body>
	
</html>